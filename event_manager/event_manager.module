<?php

/**
 * Implements hook_help().
 */
function event_manager_help($route_name, $route_match) {
  if ($route_name == 'help.page.event_manager') {
    return "Event Manager module provides event registration functionality.";
  }
}
